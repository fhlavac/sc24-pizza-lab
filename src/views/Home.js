import React from 'react';
import { Stack } from '@mui/material';
import Header from '../components/Header';
const Home = () => {
  return (
    <Stack>
      <Header />
    </Stack>
  );
};

export default Home;
