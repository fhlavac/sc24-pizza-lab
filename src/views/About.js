import React from 'react';
import { Box, Typography } from '@mui/material';

const About = () => {
  return (
    <Box>
      <Typography>About page content</Typography>
    </Box>
  );
};

export default About;
